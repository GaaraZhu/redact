# Rust
/target/

# Claude Code local settings (use `gate init` to set up hooks)
.claude/

# Project-level MCP configs (use `gate init --wrap-mcp` to set up)
.mcp.json
opencode.json

# Copilot CLI hook (use `gate init --harness copilot-cli` to set up)
.github/hooks/PreToolUse.json

# Editor
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# Env
.env
.env.local

# Python
dev/.venv/
__pycache__/
*.pyc

# Test schema
dev/scan_test_schema.json